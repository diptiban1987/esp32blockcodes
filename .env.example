# Frontend build environment variables
# Copy to .env and set values before running npm run build:prod

# Backend API URL for Arduino compile/upload.
# Leave empty for development (webpack-dev-server proxies /api/*).
# Set to your Oracle Cloud / AWS / Render backend URL for production.
BACKEND_API_URL=
